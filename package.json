{
  "name": "entropy-risk-demo",
  "version": "1.0.0",
  "description": "Entropy-based LLM answer risk analysis demo with RAG, web search, and logprobs.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check src/entropy.js && node --check src/retrieval.js && node --check src/llm.js && node --check src/risk.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.0",
    "dotenv": "^16.4.0"
  }
}
